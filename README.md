# Discord To-Do List Bot with LINE Notification

บอท Discord สำหรับจัดการรายการสิ่งที่ต้องทำ (To-Do List) ของทีม พร้อมระบบแจ้งเตือนไปยัง LINE Group โดยอัตโนมัติ

## ✨ ฟีเจอร์หลัก
- **📝 จัดการงาน**: เพิ่ม, ดูรายการ, และเคลียร์รายการงานประจำวัน
- **🔔 แจ้งเตือน LINE**: ส่งข้อความแจ้งเตือนไปที่ LINE Group ทันทีเมื่อมีการเพิ่มหรือทำงานเสร็จ
- **✅ อัปเดตสถานะ Real-time**: ปุ่มกดใน Discord จะอัปเดตสถานะงานทันทีโดยไม่ต้องพิมพ์คำสั่ง
- **📅 ระบบรีเซ็ตรายวัน**: จัดการงานแยกเป็นรายวัน (ตามเวลาประเทศไทย)
- **🔒 ปลอดภัย**: รองรับการใช้งาน Environment Variables เพื่อความปลอดภัยของ Token

## 🛠️ ข้อกำหนดเบื้องต้น (Prerequisites)
- Python 3.8 ขึ้นไป
- บัญชี LINE Developers (สำหรับ Messaging API)
- บอท Discord ที่สร้างใน Developer Portal

## 📦 การติดตั้ง (Installation)

1. **Clone โปรเจกต์**
   ```bash
   git clone https://github.com/yourusername/discord-todo-bot.git
   cd discord-todo-bot
   ```

2. **ติดตั้งไลบรารีที่จำเป็น**
   ```bash
   pip install -r requirements.txt
   ```
   *หมายเหตุ: หากยังไม่มีไฟล์ `requirements.txt` ให้สร้างไฟล์โดยมีเนื้อหาดังนี้:*
   ```text
   discord.py
   python-dotenv
   line-bot-sdk
   pytz
   ```

3. **ตั้งค่า Environment Variables**
   สร้างไฟล์ `.env` ในโฟลเดอร์เดียวกับ `todolist.py` และใส่ค่าดังนี้:
   ```env
   DISCORD_TOKEN=your_discord_bot_token_here
   LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token
   LINE_GROUP_ID=your_line_group_id
   ```
   > ⚠️ **สำคัญ**: ห้ามอัปโหลดไฟล์ `.env` ขึ้น GitHub เด็ดขาด (ตรวจสอบไฟล์ `.gitignore` ว่ามี `.env` อยู่แล้ว)

## 🚀 การใช้งาน (Usage)

รันบอทด้วยคำสั่ง:
```bash
python todolist.py
```

### คำสั่ง Slash Commands (/)
| คำสั่ง | รายละเอียด |
|---------|------------|
| `/add [task]` | เพิ่มรายการงานใหม่ลงในลิสต์รวม |
| `/list` | ดูรายการงานทั้งหมดของวันนี้ |
| `/clear` | ล้างรายการงานทั้งหมดของวันนี้ (ระวัง! ลบแล้วกู้คืนไม่ได้) |

## 📂 โครงสร้างไฟล์
```
.
├── todolist.py        # โค้ดหลักของบอท
├── todo_list.db       # ฐานข้อมูล SQLite (สร้างอัตโนมัติ)
├── .env              # ไฟล์เก็บ Token (ห้ามอัปโหลด)
├── .gitignore        # กำหนดไฟล์ที่ไม่ต้องอัปโหลดขึ้น git
└── requirements.txt  # รายชื่อไลบรารีที่ต้องใช้
```

---
**พัฒนาโดย**: TIM1Zk
